---
title: Tag
description: The Tag component is a versatile UI element used for displaying labels, categories, and status indicators with multiple variants, colors, and interactive options.
category: components
tags:
    - tag
    - component
    - label
RepoFolderName: Tags
StorybookLink: components-tags-tag--docs
---

<TagPreview />

## Usage

```tsx
import { Tag, TagVariant, TagColor, TagSize } from 'blend-v1'

function MyComponent() {
    return (
        <Tag
            text="Success"
            variant={TagVariant.SUBTLE}
            color={TagColor.SUCCESS}
            size={TagSize.MD}
            onClick={() => console.log('tag clicked')}
        />
    )
}
```

## API Reference

<DocsTypeTable
    data={[
        [
            {
                content: 'text',
                hintText: 'The text content displayed in the tag',
            },
            { content: 'string', hintText: 'string' },
            { content: '' },
        ],
        [
            { content: 'variant', hintText: 'The visual variant of the tag' },
            { content: 'TagVariant', hintText: 'enum' },
            { content: 'SUBTLE' },
        ],
        [
            { content: 'color', hintText: 'The color theme of the tag' },
            { content: 'TagColor', hintText: 'enum' },
            { content: 'PRIMARY' },
        ],
        [
            { content: 'size', hintText: 'The size of the tag' },
            { content: 'TagSize', hintText: 'enum' },
            { content: 'SM' },
        ],
        [
            {
                content: 'shape',
                hintText: 'The border radius shape of the tag',
            },
            { content: 'TagShape', hintText: 'enum' },
            { content: 'SQUARICAL' },
        ],
        [
            {
                content: 'leftSlot',
                hintText: 'Content displayed before the text',
            },
            { content: 'React.ReactNode', hintText: 'React node' },
            { content: '' },
        ],
        [
            {
                content: 'rightSlot',
                hintText: 'Content displayed after the text',
            },
            { content: 'React.ReactNode', hintText: 'React node' },
            { content: '' },
        ],
        [
            { content: 'onClick', hintText: 'Click handler function' },
            { content: '() => void', hintText: 'function' },
            { content: '' },
        ],
        [
            {
                content: 'splitTagPosition',
                hintText: 'Position for split tag styling',
            },
            { content: "'left' | 'right'", hintText: 'string union' },
            { content: '' },
        ],
    ]}
    isHoverable={true}
    className="mb-8"
    emptyMessage="No props available"
    loadingMessage="Loading props..."
/>

## Features

- Multiple tag variants (noFill, attentive, subtle)
- Six color themes (neutral, primary, success, error, warning, purple)
- Four sizes (xs, sm, md, lg)
- Two shape options (rounded, squarical)
- Optional click functionality
- Left and right slot support for icons or additional content
- Split tag positioning for special styling
- Responsive design with proper spacing
- Accessible keyboard navigation

## Usage Examples

### Basic Tag

Simple tag with text content

```tsx
<Tag text="Label" color={TagColor.PRIMARY} />
```

### Success Tag

Tag with success styling

```tsx
<Tag
    text="Success"
    variant={TagVariant.SUBTLE}
    color={TagColor.SUCCESS}
    size={TagSize.MD}
/>
```

### Interactive Tag

Clickable tag with custom handler

```tsx
<Tag
    text="Clickable"
    color={TagColor.PRIMARY}
    onClick={() => console.log('Tag clicked')}
/>
```

### Tag with Icon

Tag with left slot icon

```tsx
import { CheckCircle } from 'lucide-react'
;<Tag
    text="Completed"
    color={TagColor.SUCCESS}
    leftSlot={<CheckCircle size={12} />}
/>
```

### Warning Tag

Tag with warning styling and right slot

```tsx
import { AlertTriangle } from 'lucide-react'
;<Tag
    text="Warning"
    variant={TagVariant.ATTENTIVE}
    color={TagColor.WARNING}
    rightSlot={<AlertTriangle size={12} />}
/>
```

### Large Rounded Tag

Tag with large size and rounded shape

```tsx
<Tag
    text="Featured"
    color={TagColor.PURPLE}
    size={TagSize.LG}
    shape={TagShape.ROUNDED}
/>
```

### No Fill Tag

Tag with no background fill

```tsx
<Tag
    text="Outline"
    variant={TagVariant.NO_FILL}
    color={TagColor.NEUTRAL}
    size={TagSize.SM}
/>
```

### Split Tag

Tag with split positioning for special styling

```tsx
<Tag text="Split" color={TagColor.ERROR} splitTagPosition="left" />
```
