---
title: Checkbox
description: The Checkbox component allows users to select one or more options from a list, with support for indeterminate states, custom styling, and comprehensive accessibility features.
category: components
tags:
    - checkbox
    - component
    - form
    - input
    - selection
    - control
RepoFolderName: Checkbox
StorybookLink: components-checkbox-checkbox--docs
---

<CheckboxPreview />

## Usage

```tsx
import { Checkbox, CheckboxSize } from 'blend-v1'
import { useState } from 'react'

function MyComponent() {
    const [checked, setChecked] = useState(false)

    return (
        <Checkbox
            checked={checked}
            onCheckedChange={setChecked}
            size={CheckboxSize.MEDIUM}
            required={true}
        >
            I agree to the terms and conditions
        </Checkbox>
    )
}
```

## API Reference

<DocsTypeTable
    data={[
        [
            { content: 'id', hintText: 'Unique identifier for the checkbox' },
            { content: 'string', hintText: 'string' },
            { content: '' },
            { content: 'auto-generated' },
        ],
        [
            {
                content: 'value',
                hintText:
                    'The value associated with the checkbox when used in forms',
            },
            { content: 'string', hintText: 'string' },
            { content: '' },
            { content: '' },
        ],
        [
            {
                content: 'checked',
                hintText: 'The controlled checked state of the checkbox',
            },
            {
                content: "boolean | 'indeterminate'",
                hintText: 'boolean or string',
            },
            { content: '' },
            { content: '' },
        ],
        [
            {
                content: 'defaultChecked',
                hintText: 'The default checked state for uncontrolled usage',
            },
            { content: 'boolean', hintText: 'boolean' },
            { content: '' },
            { content: 'false' },
        ],
        [
            {
                content: 'onCheckedChange',
                hintText: 'Callback function when the checkbox state changes',
            },
            {
                content: "(checked: boolean | 'indeterminate') => void",
                hintText: 'function',
            },
            { content: '' },
            { content: '' },
        ],
        [
            {
                content: 'disabled',
                hintText:
                    'Whether the checkbox is disabled and non-interactive',
            },
            { content: 'boolean', hintText: 'boolean' },
            { content: '' },
            { content: 'false' },
        ],
        [
            {
                content: 'required',
                hintText:
                    'Whether the checkbox is required for form validation',
            },
            { content: 'boolean', hintText: 'boolean' },
            { content: '' },
            { content: 'false' },
        ],
        [
            {
                content: 'error',
                hintText: 'Whether the checkbox is in an error state',
            },
            { content: 'boolean', hintText: 'boolean' },
            { content: '' },
            { content: 'false' },
        ],
        [
            {
                content: 'size',
                hintText: 'The size variant of the checkbox',
            },
            { content: 'CheckboxSize', hintText: 'enum' },
            { content: 'SMALL, MEDIUM' },
            { content: 'CheckboxSize.MEDIUM' },
        ],
        [
            {
                content: 'children',
                hintText: 'The label content displayed next to the checkbox',
            },
            { content: 'ReactNode', hintText: 'React node' },
            { content: '' },
            { content: '' },
        ],
        [
            {
                content: 'subtext',
                hintText: 'Optional descriptive text displayed below the label',
            },
            { content: 'string', hintText: 'string' },
            { content: '' },
            { content: '' },
        ],
        [
            {
                content: 'slot',
                hintText: 'Optional content slot for additional elements',
            },
            { content: 'ReactNode', hintText: 'React node' },
            { content: '' },
            { content: '' },
        ],
    ]}
    className="mb-8"
    emptyMessage="No props available"
    loadingMessage="Loading props..."
/>

## Component Tokens

You can style the Checkbox component using the following tokens:

```tsx
export type CheckboxTokensType = {
    gap: CSSObject['gap']
    slot: {
        marginLeft: CSSObject['marginLeft']
    }
    indicator: {
        width: { [key in CheckboxSize]: CSSObject['width'] }
        height: { [key in CheckboxSize]: CSSObject['height'] }
        backgroundColor: {
            [key in CheckboxCheckedState]?: {
                [key in CheckboxInteractionState]?: CSSObject['backgroundColor']
            }
        }
        borderRadius: { [key in CheckboxSize]: CSSObject['borderRadius'] }
        border: {
            [key in CheckboxCheckedState]?: {
                [key in CheckboxInteractionState]?: CSSObject['border']
            }
        }
        outline: CSSObject['outline']
        outlineOffset: CSSObject['outlineOffset']
        boxShadow: CSSObject['boxShadow']
        icon: {
            color: {
                [key in CheckboxCheckedState]?: {
                    [key in Extract<
                        CheckboxInteractionState,
                        'default' | 'disabled'
                    >]?: CSSObject['color']
                }
            }
            width: { [key in CheckboxSize]: CSSObject['width'] }
            height: { [key in CheckboxSize]: CSSObject['height'] }
            strokeWidth: { [key in CheckboxSize]: CSSObject['strokeWidth'] }
        }
    }
    content: {
        gap: CSSObject['gap']
        label: {
            color: {
                [key in CheckboxInteractionState]: CSSObject['color']
            }
            fontSize: { [key in CheckboxSize]: CSSObject['fontSize'] }
            fontWeight: { [key in CheckboxSize]: CSSObject['fontWeight'] }
        }
        subtext: {
            color: {
                [key in CheckboxInteractionState]: CSSObject['color']
            }
            fontSize: { [key in CheckboxSize]: CSSObject['fontSize'] }
            fontWeight: { [key in CheckboxSize]: CSSObject['fontWeight'] }
        }
    }
    required: {
        color: CSSObject['color']
    }
}

export type ResponsiveCheckboxTokens = {
    [key in keyof BreakpointType]: CheckboxTokensType
}
```
