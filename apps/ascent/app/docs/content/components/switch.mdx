---
title: Switch
description: The Switch component provides a toggle control for enabling or disabling features, with smooth animations and clear visual feedback for binary state changes.
category: components
tags:
    - switch
    - component
    - toggle
    - form
    - input
RepoFolderName: Switch
StorybookLink: components-switch-switch--docs
---

<SwitchPreview />

## Usage

```tsx
import { Switch, SwitchGroup, SwitchSize } from 'blend-v1'

function MyComponent() {
    const [isEnabled, setIsEnabled] = useState(false)

    return (
        <Switch
            checked={isEnabled}
            onChange={setIsEnabled}
            label="Enable notifications"
            size={SwitchSize.MEDIUM}
        />
    )
}
```

## API Reference

### Switch Props

<DocsTypeTable
    data={[
        [
            {
                content: 'Switch.id',
                hintText:
                    'Optional unique identifier string for the switch element. Used for form identification, accessibility, and linking labels. If not provided, an ID is automatically generated. Should be unique within the form.',
            },
            {
                content: 'string',
                hintText: 'Unique identifier for the switch element',
            },
            { content: '' },
        ],
        [
            {
                content: 'Switch.checked',
                hintText:
                    'Optional boolean that controls whether the switch is in the on/checked state (controlled mode). When true, the switch is on/checked. When false, the switch is off/unchecked. Use this with onChange for controlled state management. If not provided, use defaultChecked for uncontrolled mode.',
            },
            {
                content: 'boolean',
                hintText:
                    'true shows checked state, false shows unchecked state',
            },
            { content: '' },
        ],
        [
            {
                content: 'Switch.defaultChecked',
                hintText:
                    'Optional boolean that sets the default checked state for uncontrolled usage. When true, the switch starts in the checked state. When false, the switch starts in the unchecked state. Only used when checked is not provided. Defaults to false.',
            },
            {
                content: 'boolean',
                hintText:
                    'true sets initial checked state, false sets initial unchecked state',
            },
            { content: '' },
        ],
        [
            {
                content: 'Switch.onChange',
                hintText:
                    'Optional callback function invoked when the switch state changes. Receives a boolean indicating the new checked state. Use this to update your state when using controlled mode. The function is called when the user toggles the switch.',
            },
            {
                content: '(checked: boolean) => void',
                hintText:
                    'Function called with new checked state when switch state changes',
            },
            { content: '' },
        ],
        [
            {
                content: 'Switch.disabled',
                hintText:
                    "When true, disables the switch, making it non-interactive and visually muted. Users cannot toggle the switch. Disabled switches show reduced opacity and cannot be clicked. Useful for preventing interaction when prerequisites aren't met. Defaults to false.",
            },
            {
                content: 'boolean',
                hintText:
                    'true disables switch, false allows normal interaction',
            },
            { content: '' },
        ],
        [
            {
                content: 'Switch.required',
                hintText:
                    'When true, marks the switch as required and displays an asterisk (*) next to the label. Useful for form validation. Required switches must be checked before form submission. Defaults to false.',
            },
            {
                content: 'boolean',
                hintText:
                    'true marks switch as required, false allows optional selection',
            },
            { content: '' },
        ],
        [
            {
                content: 'Switch.error',
                hintText:
                    'When true, displays the switch in an error state with red border and error styling. Useful for form validation feedback when the switch state is invalid or missing (for required switches). Defaults to false.',
            },
            {
                content: 'boolean',
                hintText: 'true shows error state, false shows normal state',
            },
            { content: '' },
        ],
        [
            {
                content: 'Switch.size',
                hintText:
                    'Optional SwitchSize enum value that determines the size variant of the switch. Affects the switch container dimensions, thumb size, and label font size. SMALL creates a compact switch, MEDIUM creates a standard size. Defaults to MEDIUM.',
            },
            {
                content: 'SwitchSize',
                hintText: 'Enum that determines the size variant of the switch',
            },
            { content: 'SMALL, MEDIUM' },
        ],
        [
            {
                content: 'Switch.label',
                hintText:
                    'Optional label string displayed next to the switch. Provides context and description of what the switch controls (e.g., "Enable notifications", "Auto-save"). The label is clickable and toggles the switch. Should be concise and descriptive.',
            },
            {
                content: 'string',
                hintText: 'Label text displayed next to the switch',
            },
            { content: '' },
        ],
        [
            {
                content: 'Switch.subtext',
                hintText:
                    "Optional React element displayed below the main label. Provides additional context, explanation, or clarification about the switch. Can be a string or React element. Styled with smaller, lighter text. Useful for explaining the switch's purpose or consequences.",
            },
            {
                content: 'ReactNode',
                hintText: 'React element displayed below the main label',
            },
            { content: '' },
        ],
        [
            {
                content: 'Switch.slot',
                hintText:
                    'Optional React element displayed alongside the label, typically on the right side. Can be used for badges, icons, or other supplementary content that complements the switch. Useful for displaying additional information or actions.',
            },
            {
                content: 'ReactNode',
                hintText: 'React element displayed alongside the switch label',
            },
            { content: '' },
        ],
        [
            {
                content: 'Switch.name',
                hintText:
                    'Optional name attribute string for form grouping. Used for form identification, form submission, and accessibility. Should be unique within the form. Useful when switches are part of a form that needs to be submitted.',
            },
            {
                content: 'string',
                hintText: 'Name attribute for form grouping and identification',
            },
            { content: '' },
        ],
        [
            {
                content: 'Switch.value',
                hintText:
                    'Optional value string associated with the switch. Used for form submission and when the switch is part of a SwitchGroup. The value is included in form data when the switch is checked. Useful for identifying which switch was toggled in a group.',
            },
            {
                content: 'string',
                hintText: 'Value string associated with the switch',
            },
            { content: '' },
        ],
    ]}
    className="mb-8"
    emptyMessage="No props available"
    loadingMessage="Loading props..."
/>

### SwitchGroup Props

<DocsTypeTable
    data={[
        [
            {
                content: 'SwitchGroup.id',
                hintText:
                    'Optional unique identifier string for the switch group container. Used for form identification, accessibility, and linking labels. Should be unique within the form. If not provided, an ID may be auto-generated.',
            },
            {
                content: 'string',
                hintText: 'Unique identifier for the switch group element',
            },
            { content: '' },
        ],
        [
            {
                content: 'SwitchGroup.label',
                hintText:
                    'Optional label string displayed above the switch group. Provides context and description of what users should select (e.g., "Notification preferences", "Feature toggles"). The label is styled prominently and helps users understand the selection.',
            },
            {
                content: 'string',
                hintText: 'Label text displayed above the switch group',
            },
            { content: '' },
        ],
        [
            {
                content: 'SwitchGroup.name',
                hintText:
                    'Optional name attribute string for the switch group. Used for form identification, form submission, and accessibility. Should be unique within the form. Useful for form libraries and native form handling.',
            },
            {
                content: 'string',
                hintText: 'Name attribute for form grouping and identification',
            },
            { content: '' },
        ],
        [
            {
                content: 'SwitchGroup.children',
                hintText:
                    'Required React nodes representing the Switch components to render within the group. Each Switch should have a unique value prop. The SwitchGroup manages the selection state and tracks which switches are checked. The children are rendered vertically with proper spacing.',
            },
            {
                content: 'ReactNode',
                hintText:
                    'React elements (Switch components) to render in the group',
            },
            { content: '' },
        ],
        [
            {
                content: 'SwitchGroup.disabled',
                hintText:
                    "When true, disables all switch components within the group, making them non-interactive and visually muted. Disabled switches show reduced opacity and cannot be toggled. Useful for preventing interaction when prerequisites aren't met. Defaults to false.",
            },
            {
                content: 'boolean',
                hintText:
                    'true disables all switches in group, false allows normal interaction',
            },
            { content: '' },
        ],
        [
            {
                content: 'SwitchGroup.value',
                hintText:
                    'Optional controlled array of selected value strings. When provided, the SwitchGroup is controlled and selection is managed by your component state. The array should contain the value strings of all checked switches. Required for controlled mode.',
            },
            {
                content: 'string[]',
                hintText:
                    'Array of value strings representing checked switches',
            },
            { content: '' },
        ],
        [
            {
                content: 'SwitchGroup.defaultValue',
                hintText:
                    'Optional default array of selected value strings for uncontrolled usage. When provided, the SwitchGroup manages its own state internally. The array should contain the value strings of switches that should be initially checked. Defaults to an empty array (no switches checked).',
            },
            {
                content: 'string[]',
                hintText:
                    'Array of value strings for initially checked switches',
            },
            { content: '' },
        ],
        [
            {
                content: 'SwitchGroup.onChange',
                hintText:
                    'Optional callback function invoked when the selected values change. Receives an array of value strings representing all currently checked switches. Use this to update your state when using controlled mode. The function is called whenever a switch is toggled.',
            },
            {
                content: '(value: string[]) => void',
                hintText:
                    'Function called with new value array when selection changes',
            },
            { content: '' },
        ],
    ]}
    className="mb-8"
    emptyMessage="No props available"
    loadingMessage="Loading props..."
/>

## Component Tokens

You can style the Switch component using the following tokens:

```tsx
export type SwitchState = 'default' | 'hover' | 'disabled' | 'error'
export type SwitchVariant = 'active' | 'inactive'

export type SwitchTokensType = {
    gap: CSSObject['gap']

    switchContainer: {
        height: {
            [key in SwitchSize]: CSSObject['height']
        }
        width: {
            [key in SwitchSize]: CSSObject['width']
        }
        borderRadius: {
            [key in SwitchSize]: CSSObject['borderRadius']
        }
        backgroundColor: {
            [key in SwitchVariant]: {
                [key in SwitchState]: CSSObject['backgroundColor']
            }
        }
        thumb: {
            marginTop: CSSObject['marginTop']
            backgroundColor: CSSObject['backgroundColor']
            border: {
                color: CSSObject['borderColor']
                width: CSSObject['borderWidth']
            }
            borderRadius: {
                [key in SwitchSize]: CSSObject['borderRadius']
            }
            size: {
                [key in SwitchSize]: {
                    width: CSSObject['width']
                    height: CSSObject['height']
                    top: CSSObject['top']
                    left: CSSObject['left']
                    offset: {
                        active: CSSObject['left']
                        inactive: CSSObject['left']
                    }
                }
            }
            outline: CSSObject['outline']
            outlineOffset: CSSObject['outlineOffset']
            boxShadow: CSSObject['boxShadow']
        }
    }

    content: {
        label: {
            color: {
                [key in SwitchState]: CSSObject['color']
            }
            fontSize: {
                [key in SwitchSize]: CSSObject['fontSize']
            }
            fontWeight: {
                [key in SwitchSize]: CSSObject['fontWeight']
            }
            gap: CSSObject['gap']
        }
        subtext: {
            color: {
                [key in SwitchState]: CSSObject['color']
            }
            fontSize: {
                [key in SwitchSize]: CSSObject['fontSize']
            }
            fontWeight: {
                [key in SwitchSize]: CSSObject['fontWeight']
            }
        }
        gap: CSSObject['gap']
    }
    required: {
        color: CSSObject['color']
    }
}

export type ResponsiveSwitchTokens = {
    [key in keyof BreakpointType]: SwitchTokensType
}
```
