---
title: Checkbox
description: The Checkbox component allows users to select one or more options from a list, with support for indeterminate states, custom styling, and comprehensive accessibility features.
category: components
tags:
    - checkbox
    - component
    - form
    - input
    - selection
    - control
RepoFolderName: Checkbox
StorybookLink: components-checkbox-checkbox--docs
---

<CheckboxPreview />

## Usage

```tsx
import { Checkbox, CheckboxSize } from 'blend-v1'
import { useState } from 'react'

function MyComponent() {
    const [checked, setChecked] = useState(false)

    return (
        <Checkbox
            checked={checked}
            onCheckedChange={setChecked}
            size={CheckboxSize.MEDIUM}
            required={true}
        >
            I agree to the terms and conditions
        </Checkbox>
    )
}
```

## API Reference

<DocsTypeTable
    data={[
        [
            {
                content: 'id',
                hintText:
                    "Optional unique identifier string for the checkbox. If not provided, the component automatically generates a unique ID using React's useId hook. The ID is used for form association, accessibility, and linking labels to the checkbox input.",
            },
            {
                content: 'string',
                hintText: 'Unique identifier string for the checkbox element',
            },
            { content: '' },
        ],
        [
            {
                content: 'value',
                hintText:
                    'Optional value string associated with the checkbox when used in forms. When the checkbox is checked and submitted in a form, this value is included in the form data. Useful for identifying which checkbox option was selected.',
            },
            {
                content: 'string',
                hintText:
                    'Value submitted with form data when checkbox is checked',
            },
            { content: '' },
        ],
        [
            {
                content: 'checked',
                hintText:
                    'Optional controlled state for whether the checkbox is checked. Can be true (checked), false (unchecked), or "indeterminate" (partially checked, typically for parent checkboxes with mixed child states). When provided, the component works in controlled mode, requiring onCheckedChange.',
            },
            {
                content: "boolean | 'indeterminate'",
                hintText:
                    'true for checked, false for unchecked, "indeterminate" for mixed state',
            },
            { content: '' },
        ],
        [
            {
                content: 'defaultChecked',
                hintText:
                    "Optional initial checked state for uncontrolled usage. Sets whether the checkbox starts checked or unchecked when the component first mounts. Use this for uncontrolled components where you don't need to manage state externally. Defaults to false.",
            },
            {
                content: 'boolean',
                hintText: 'true starts checked, false starts unchecked',
            },
            { content: '' },
        ],
        [
            {
                content: 'onCheckedChange',
                hintText:
                    'Optional callback function invoked when the checkbox state changes (user clicks or programmatically changed). Receives the new checked state (boolean or "indeterminate") as a parameter. Required for controlled usage to update state.',
            },
            {
                content: "(checked: boolean | 'indeterminate') => void",
                hintText:
                    'Function called with new checked state when it changes',
            },
            { content: '' },
        ],
        [
            {
                content: 'disabled',
                hintText:
                    "When true, disables the checkbox, making it non-interactive and visually muted. Disabled checkboxes cannot be clicked and are excluded from form submissions. Useful for preventing interaction when prerequisites aren't met. Defaults to false.",
            },
            {
                content: 'boolean',
                hintText:
                    'true disables interaction, false allows normal interaction',
            },
            { content: '' },
        ],
        [
            {
                content: 'required',
                hintText:
                    'When true, marks the checkbox as required for form validation. Browsers and form libraries will validate that the checkbox is checked before allowing form submission. Visual indicators (asterisk) may be displayed. Defaults to false.',
            },
            {
                content: 'boolean',
                hintText:
                    'true makes checkbox required, false makes it optional',
            },
            { content: '' },
        ],
        [
            {
                content: 'error',
                hintText:
                    'When true, displays the checkbox in an error state with error styling (typically red border/indicator). Useful for form validation feedback when the checkbox is required but unchecked, or when validation fails. Defaults to false.',
            },
            {
                content: 'boolean',
                hintText: 'true shows error state, false shows normal state',
            },
            { content: '' },
        ],
        [
            {
                content: 'size',
                hintText:
                    'Controls the dimensions of the checkbox indicator. SMALL creates a compact checkbox suitable for dense layouts, MEDIUM provides standard sizing for most use cases. Size affects both the checkbox box and the checkmark icon. Defaults to MEDIUM.',
            },
            {
                content: 'CheckboxSize',
                hintText:
                    'Enum that determines the physical dimensions of the checkbox',
            },
            { content: 'SMALL, MEDIUM' },
        ],
        [
            {
                content: 'children',
                hintText:
                    'Optional React node displayed as the label text next to the checkbox. Typically a string but can be any React content. Clicking the label text also toggles the checkbox. The label is associated with the checkbox for accessibility.',
            },
            {
                content: 'ReactNode',
                hintText:
                    'Any React content (text, elements) to display as the label',
            },
            { content: '' },
        ],
        [
            {
                content: 'subtext',
                hintText:
                    'Optional descriptive text string displayed below the main label. Provides additional context, explanation, or clarification about the checkbox option. Styled with smaller, lighter text than the main label.',
            },
            {
                content: 'string',
                hintText: 'Secondary descriptive text shown below the label',
            },
            { content: '' },
        ],
        [
            {
                content: 'slot',
                hintText:
                    'Optional React element displayed to the right of the label and subtext. Useful for adding icons, badges, links, or other supplementary content that should appear after the checkbox label area.',
            },
            {
                content: 'ReactNode',
                hintText:
                    'Any React element to display after the label content',
            },
            { content: '' },
        ],
    ]}
    className="mb-8"
    emptyMessage="No props available"
    loadingMessage="Loading props..."
/>

## Component Tokens

You can style the Checkbox component using the following tokens:

```tsx
export type CheckboxTokensType = {
    gap: CSSObject['gap']
    slot: {
        marginLeft: CSSObject['marginLeft']
    }
    indicator: {
        width: { [key in CheckboxSize]: CSSObject['width'] }
        height: { [key in CheckboxSize]: CSSObject['height'] }
        backgroundColor: {
            [key in CheckboxCheckedState]?: {
                [key in CheckboxInteractionState]?: CSSObject['backgroundColor']
            }
        }
        borderRadius: { [key in CheckboxSize]: CSSObject['borderRadius'] }
        border: {
            [key in CheckboxCheckedState]?: {
                [key in CheckboxInteractionState]?: CSSObject['border']
            }
        }
        outline: CSSObject['outline']
        outlineOffset: CSSObject['outlineOffset']
        boxShadow: CSSObject['boxShadow']
        icon: {
            color: {
                [key in CheckboxCheckedState]?: {
                    [key in Extract<
                        CheckboxInteractionState,
                        'default' | 'disabled'
                    >]?: CSSObject['color']
                }
            }
            width: { [key in CheckboxSize]: CSSObject['width'] }
            height: { [key in CheckboxSize]: CSSObject['height'] }
            strokeWidth: { [key in CheckboxSize]: CSSObject['strokeWidth'] }
        }
    }
    content: {
        gap: CSSObject['gap']
        label: {
            color: {
                [key in CheckboxInteractionState]: CSSObject['color']
            }
            fontSize: { [key in CheckboxSize]: CSSObject['fontSize'] }
            fontWeight: { [key in CheckboxSize]: CSSObject['fontWeight'] }
        }
        subtext: {
            color: {
                [key in CheckboxInteractionState]: CSSObject['color']
            }
            fontSize: { [key in CheckboxSize]: CSSObject['fontSize'] }
            fontWeight: { [key in CheckboxSize]: CSSObject['fontWeight'] }
        }
    }
    required: {
        color: CSSObject['color']
    }
}

export type ResponsiveCheckboxTokens = {
    [key in keyof BreakpointType]: CheckboxTokensType
}
```
