---
title: Slider
description: The Slider component provides an interactive range input control for selecting values within a specified range, supporting single and multi-thumb configurations with customizable styling and value formatting.
category: components
tags:
    - slider
    - component
    - input
    - range
    - control
    - interactive
    - value
    - selection
RepoFolderName: Slider
StorybookLink: components-slider--docs
---

<SliderPreview />

## Usage

```tsx
import { Slider, SliderVariant, SliderSize, SliderValueType } from 'blend-v1'
import { useState } from 'react'

function MyComponent() {
    const [value, setValue] = useState([25])

    return (
        <div>
            <label className="block text-sm font-medium mb-2">
                Volume: {value[0]}%
            </label>
            <Slider
                variant={SliderVariant.PRIMARY}
                size={SliderSize.MEDIUM}
                value={value}
                onValueChange={setValue}
                min={0}
                max={100}
                step={1}
                showValueLabels={true}
                labelPosition="top"
                valueFormat={{
                    type: SliderValueType.NUMBER,
                    suffix: '%',
                }}
            />
        </div>
    )
}
```

## API Reference

<DocsTypeTable
    data={[
        [
            {
                content: 'Slider.variant',
                hintText:
                    'Optional SliderVariant enum value that determines the visual style variant of the slider. PRIMARY creates a prominent slider with emphasized styling, SECONDARY creates a more subtle slider. The variant affects the track and thumb colors. Defaults to PRIMARY.',
            },
            {
                content: 'SliderVariant',
                hintText:
                    'Enum that determines the visual style variant of the slider',
            },
            { content: 'PRIMARY, SECONDARY' },
        ],
        [
            {
                content: 'Slider.size',
                hintText:
                    'Optional SliderSize enum value that determines the size variant affecting dimensions, spacing, track height, and thumb size. SMALL creates a compact slider, MEDIUM creates a standard size, LARGE creates a prominent size. Defaults to MEDIUM.',
            },
            {
                content: 'SliderSize',
                hintText: 'Enum that determines the size variant of the slider',
            },
            { content: 'SMALL, MEDIUM, LARGE' },
        ],
        [
            {
                content: 'Slider.value',
                hintText:
                    'Optional array of numbers representing the controlled value(s) for the slider. For single-thumb sliders, provide an array with one value (e.g., [50]). For range sliders, provide two values (e.g., [25, 75]). The array length determines the number of thumbs. Use this for controlled state management. If not provided, use defaultValue.',
            },
            {
                content: 'number[]',
                hintText: 'Array of numbers representing slider value(s)',
            },
            { content: '' },
        ],
        [
            {
                content: 'Slider.defaultValue',
                hintText:
                    'Optional array of numbers representing the default value(s) for uncontrolled usage. For single-thumb sliders, provide an array with one value (e.g., [50]). For range sliders, provide two values (e.g., [25, 75]). The slider manages its own state internally. Defaults to [0] (single thumb at minimum).',
            },
            {
                content: 'number[]',
                hintText:
                    'Array of numbers representing default slider value(s)',
            },
            { content: '' },
        ],
        [
            {
                content: 'Slider.onValueChange',
                hintText:
                    'Optional callback function invoked when the slider value changes. Receives an array of numbers representing the new value(s). Use this to update your state when using controlled mode. The function is called as the user drags the thumb(s) or clicks on the track.',
            },
            {
                content: '(value: number[]) => void',
                hintText:
                    'Function called with new value array when slider value changes',
            },
            { content: '' },
        ],
        [
            {
                content: 'Slider.min',
                hintText:
                    'Optional minimum value number for the slider range. The slider cannot go below this value. The thumb(s) will be constrained to values >= min. Useful for setting lower bounds. Defaults to 0.',
            },
            {
                content: 'number',
                hintText:
                    'Number representing the minimum value of the slider range',
            },
            { content: '' },
        ],
        [
            {
                content: 'Slider.max',
                hintText:
                    'Optional maximum value number for the slider range. The slider cannot go above this value. The thumb(s) will be constrained to values <= max. Useful for setting upper bounds. Defaults to 100.',
            },
            {
                content: 'number',
                hintText:
                    'Number representing the maximum value of the slider range',
            },
            { content: '' },
        ],
        [
            {
                content: 'Slider.step',
                hintText:
                    'Optional step increment number for value changes. When the user drags the thumb or uses arrow keys, the value changes by this increment. Useful for discrete value selection (e.g., step={5} for multiples of 5). Must be a positive number. Defaults to 1.',
            },
            {
                content: 'number',
                hintText: 'Positive number representing the step increment',
            },
            { content: '' },
        ],
        [
            {
                content: 'Slider.showValueLabels',
                hintText:
                    'When true, displays value labels on or near the thumb(s) showing the current value. The labels appear based on the labelPosition prop. Useful for providing precise feedback about the selected value. The labels are formatted according to valueFormat if provided. Defaults to false.',
            },
            {
                content: 'boolean',
                hintText: 'true shows value labels, false hides value labels',
            },
            { content: '' },
        ],
        [
            {
                content: 'Slider.labelPosition',
                hintText:
                    'Optional string that determines the position of value labels relative to thumbs. Valid values: "top" (labels appear above thumbs), "bottom" (labels appear below thumbs), "inline" (labels appear on the thumb itself). Only applies when showValueLabels is true. Defaults to "top".',
            },
            {
                content: "'top' | 'bottom' | 'inline'",
                hintText: 'String that determines the position of value labels',
            },
            { content: 'top, bottom, inline' },
        ],
        [
            {
                content: 'Slider.valueFormat',
                hintText:
                    'Optional SliderValueFormatConfig object that configures how values are formatted and displayed. Can specify number type (NUMBER, DECIMAL), decimal places, prefix/suffix strings (e.g., "$", "%"), custom formatter function, and whether to show labels. Useful for currency, percentage, or custom formatting. If not provided, values are displayed as plain numbers.',
            },
            {
                content: 'SliderValueFormatConfig',
                hintText:
                    'Object with value formatting configuration properties',
            },
            { content: '' },
        ],
        [
            {
                content: 'Slider.disabled',
                hintText:
                    "When true, disables the slider, making it non-interactive and visually muted. Users cannot drag the thumb(s) or change values. Disabled sliders show reduced opacity. Useful for preventing interaction when prerequisites aren't met. Defaults to false.",
            },
            {
                content: 'boolean',
                hintText:
                    'true disables slider, false allows normal interaction',
            },
            { content: '' },
        ],
        [
            {
                content: 'Slider.orientation',
                hintText:
                    'Optional string that determines the orientation of the slider. Valid values: "horizontal" (slider extends left-to-right, default), "vertical" (slider extends top-to-bottom). Vertical sliders are useful for height, volume, or other vertical metrics. Defaults to "horizontal".',
            },
            {
                content: "'horizontal' | 'vertical'",
                hintText: 'String that determines the slider orientation',
            },
            { content: 'horizontal, vertical' },
        ],
    ]}
    className="mb-8"
    emptyMessage="No props available"
    loadingMessage="Loading props..."
/>

## Component Tokens

You can style the Slider component using the following tokens:

```tsx
export type SliderTokensType = {
    root: {
        height: CSSObject['height']
        cursor: CSSObject['cursor']
    }
    track: {
        backgroundColor: CSSObject['backgroundColor']
        borderRadius: CSSObject['borderRadius']
        height: CSSObject['height']
    }
    range: {
        backgroundColor: CSSObject['backgroundColor']
        borderRadius: CSSObject['borderRadius']
        height: CSSObject['height']
    }
    thumb: {
        width: CSSObject['width']
        height: CSSObject['height']
        backgroundColor: CSSObject['backgroundColor']
        border: CSSObject['border']
        borderRadius: CSSObject['borderRadius']
        boxShadow: CSSObject['boxShadow']
        cursor: CSSObject['cursor']
        outline: CSSObject['outline']
        outlineOffset: CSSObject['outlineOffset']
    }
    label: {
        fontSize: CSSObject['fontSize']
        fontWeight: CSSObject['fontWeight']
        color: CSSObject['color']
        padding: CSSObject['padding']
        borderRadius: CSSObject['borderRadius']
        backgroundColor: CSSObject['backgroundColor']
        border: CSSObject['border']
        boxShadow: CSSObject['boxShadow']
    }
}
```
