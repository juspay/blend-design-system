---
title: Breadcrumb
description: The Breadcrumb component provides navigation context by showing the current page's location within a site hierarchy, helping users understand where they are and navigate back.
category: components
tags:
    - breadcrumb
    - component
    - navigation
    - hierarchy
    - path
RepoFolderName: Breadcrumb
StorybookLink: components-breadcrumb--docs
---

<BreadcrumbPreview />

## Usage

```tsx
import { Breadcrumb, BreadcrumbItemType } from 'blend-v1'

function MyComponent() {
    const breadcrumbItems: BreadcrumbItemType[] = [
        { label: 'Home', href: '/' },
        { label: 'Products', href: '/products' },
        { label: 'Electronics', href: '/products/electronics' },
        { label: 'Smartphones', href: '/products/electronics/smartphones' },
    ]

    return <Breadcrumb items={breadcrumbItems} />
}
```

## API Reference

<DocsTypeTable
    data={[
        [
            {
                content: 'items',
                hintText:
                    'Required array of breadcrumb items representing the hierarchical navigation path from root to current page. Each item should have a label (display text), href (navigation URL), and optionally leftSlot/rightSlot for custom content. The component automatically handles overflow by showing the first item, collapsing middle items into a menu when there are more than 4 items, and displaying the last 3 items.',
            },
            {
                content: 'BreadcrumbItemType[]',
                hintText:
                    'Array of objects with label, href, and optional leftSlot/rightSlot properties',
            },
            { content: '' },
        ],
    ]}
    className="mb-8"
    emptyMessage="No props available"
    loadingMessage="Loading props..."
/>

### BreadcrumbItemType

Each item in the `items` array should have the following structure:

<DocsTypeTable
    data={[
        [
            {
                content: 'label',
                hintText:
                    'Required text string displayed as the breadcrumb item label. This is the visible text that users see and click to navigate. Should be concise and descriptive of the page or section it represents.',
            },
            {
                content: 'string',
                hintText: 'Text content displayed as the breadcrumb item label',
            },
            { content: '' },
        ],
        [
            {
                content: 'href',
                hintText:
                    'Required URL string that the breadcrumb item links to. When clicked, users will navigate to this URL. For the active (last) item, this is typically omitted or set to the current page URL, and the item becomes non-clickable.',
            },
            {
                content: 'string',
                hintText: 'URL path that the breadcrumb item navigates to',
            },
            { content: '' },
        ],
        [
            {
                content: 'leftSlot',
                hintText:
                    'Optional React element displayed before (to the left of) the label text. Commonly used for icons, badges, or other visual indicators that provide additional context about the breadcrumb item.',
            },
            {
                content: 'React.ReactNode',
                hintText: 'Any React element to display before the label text',
            },
            { content: '' },
        ],
        [
            {
                content: 'rightSlot',
                hintText:
                    'Optional React element displayed after (to the right of) the label text. Useful for adding icons, badges, dropdown indicators, or other supplementary content that appears after the label.',
            },
            {
                content: 'React.ReactNode',
                hintText: 'Any React element to display after the label text',
            },
            { content: '' },
        ],
    ]}
    className="mb-8"
    emptyMessage="No props available"
    loadingMessage="Loading props..."
/>

## Component Tokens

You can style the breadcrumb component using the following tokens:

```tsx
export type BreadcrumbTokenType = {
    gap: CSSObject['gap']
    item: {
        padding: CSSObject['padding']
        gap: CSSObject['gap']
        text: {
            fontSize: CSSObject['fontSize']
            fontWeight: CSSObject['fontWeight']
            color: {
                [key in 'default' | 'hover' | 'active']: CSSObject['color']
            }
        }
    }
}
```
