/**
 * Compatibility shim: re-exports the responsive Select tokens dispatcher and
 * the deprecated `selectTokens` default value from `./select.tokens`.
 */
export * from './select.tokens'
export { default } from './select.tokens'
