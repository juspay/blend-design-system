---
title: Button
description: The Button component is a fundamental UI element used for user interactions.
category: components
tags:
    - button
    - component
RepoFolderName: Button
StorybookLink: components-button-buttonv2--docs
---

<ButtonPreview />

## Usage

```tsx
import { Button, ButtonVariant, ButtonSize } from 'blend-v1'

function MyComponent() {
    return (
        <Button
            variant={ButtonVariant.Primary}
            size={ButtonSize.Medium}
            text="Click me"
            onClick={() => console.log('clicked')}
        />
    )
}
```

## API Reference

<DocsTypeTable
    data={[
        [
            {
                content: 'buttonType',
                hintText: 'The visual type/variant of the button',
            },
            { content: 'ButtonType', hintText: 'enum' },
            { content: 'ButtonType.PRIMARY' },
        ],
        [
            { content: 'size', hintText: 'The size of the button' },
            { content: 'ButtonSize', hintText: 'enum' },
            { content: 'ButtonSize.SMALL' },
        ],
        [
            {
                content: 'subType',
                hintText:
                    'The subtype that affects button styling and behavior',
            },
            { content: 'ButtonSubType', hintText: 'enum' },
            { content: 'ButtonSubType.DEFAULT' },
        ],
        [
            {
                content: 'text',
                hintText: 'The text content displayed in the button',
            },
            { content: 'string', hintText: 'string' },
            { content: '' },
        ],
        [
            {
                content: 'leadingIcon',
                hintText: 'Icon element to display before the button text',
            },
            { content: 'React.ReactNode', hintText: 'React node' },
            { content: '' },
        ],
        [
            {
                content: 'trailingIcon',
                hintText: 'Icon element to display after the button text',
            },
            { content: 'React.ReactNode', hintText: 'React node' },
            { content: '' },
        ],
        [
            {
                content: 'disabled',
                hintText: 'Whether the button is disabled and non-interactive',
            },
            { content: 'boolean', hintText: 'boolean' },
            { content: 'false' },
        ],
        [
            {
                content: 'onClick',
                hintText: 'Callback function called when the button is clicked',
            },
            { content: '() => void', hintText: 'function' },
            { content: '' },
        ],
        [
            {
                content: 'loading',
                hintText:
                    'Whether the button is in a loading state with spinner',
            },
            { content: 'boolean', hintText: 'boolean' },
            { content: 'false' },
        ],
        [
            {
                content: 'buttonGroupPosition',
                hintText: 'Position of the button within a button group',
            },
            { content: "'center' | 'left' | 'right'", hintText: 'union type' },
            { content: '' },
        ],
        [
            {
                content: 'fullWidth',
                hintText:
                    'Whether the button should take the full width of its container',
            },
            { content: 'boolean', hintText: 'boolean' },
            { content: 'false' },
        ],
        [
            {
                content: 'justifyContent',
                hintText:
                    'CSS justify-content property for button content alignment',
            },
            {
                content: "CSSObject['justifyContent']",
                hintText: 'CSS property',
            },
            { content: 'center' },
        ],
    ]}
    className="mb-8"
    emptyMessage="No props available"
    loadingMessage="Loading props..."
/>

## Features

- Multiple button types (primary, secondary, danger, success)
- Three sizes (small, medium, large)
- Loading and disabled states
- Icon support (leading and trailing)
- Full width option
- Flexible content alignment
- Button group positioning support

## Usage Examples

### Basic Button

Simple button with text

```tsx
<Button text="Click me" buttonType={ButtonType.PRIMARY} />
```

### Button with Icons

Button with leading and trailing icons

```tsx
<Button
    text="Save"
    leadingIcon={<SaveIcon />}
    buttonType={ButtonType.SUCCESS}
/>
```

### Loading Button

Button in loading state

```tsx
<Button text="Processing..." loading={true} buttonType={ButtonType.PRIMARY} />
```

### Full Width Button

Button that takes full width of container

```tsx
<Button text="Full Width" fullWidth={true} buttonType={ButtonType.SECONDARY} />
```

## Component Tokens

You can style the button component using the following tokens:

```tsx
export type ButtonState = 'default' | 'hover' | 'active' | 'disabled'

export type ButtonTokensType = {
    gap: CSSObject['gap']
    backgroundColor: {
        [key in ButtonType]: {
            [key in ButtonSubType]: {
                [key in ButtonState]: CSSObject['background']
            }
        }
    }
    color: {
        [key in ButtonType]: {
            [key in ButtonSubType]: {
                [key in ButtonState]: CSSObject['color']
            }
        }
    }
    borderRadius: {
        [key in ButtonType]: {
            [key in ButtonSubType]: {
                [key in ButtonState]: CSSObject['borderRadius']
            }
        }
    }
    padding: {
        [key in ButtonSize]: {
            [key in ButtonSubType]: CSSObject['padding']
        }
    }
    border: {
        [key in ButtonType]: {
            [key in ButtonSubType]: {
                [key in ButtonState]: CSSObject['border']
            }
        }
    }
    shadow: {
        [key in ButtonType]: {
            [key in ButtonSubType]: {
                [key in ButtonState]: CSSObject['boxShadow']
            }
        }
    }
    outline: {
        [key in ButtonType]: {
            [key in ButtonSubType]: {
                [key in ButtonState]: CSSObject['outline']
            }
        }
    }
}
```
