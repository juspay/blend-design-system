import { DropdownInputProps } from './types'
declare const DropdownInput: ({
    label,
    sublabel,
    disabled,
    helpIconHintText,
    name,
    required,
    error,
    errorMessage,
    hintText,
    value,
    onChange,
    slot,
    size,
    placeholder,
    dropDownValue,
    onDropDownChange,
    dropDownItems,
    ...rest
}: DropdownInputProps) => import('react/jsx-runtime').JSX.Element
export default DropdownInput
