---
title: Button
description: The Button component is a fundamental UI element used for user interactions, supporting multiple variants, sizes, and states.
category: components
tags:
    - button
    - component
    - interaction
    - action
    - primary
RepoFolderName: Button
StorybookLink: components-button-buttonv2--docs
---

<ButtonPreview />

## Usage

```tsx
import { Button, ButtonType, ButtonSize, ButtonSubType } from 'blend-v1'

function MyComponent() {
    return (
        <Button
            buttonType={ButtonType.PRIMARY}
            size={ButtonSize.MEDIUM}
            text="Get Started"
            onClick={() => console.log('Button clicked')}
        />
    )
}
```

## API Reference

<DocsTypeTable
    data={[
        [
            {
                content: 'buttonType',
                hintText:
                    'Defines the visual style and semantic meaning of the button. PRIMARY (blue) for main actions, SECONDARY (outlined) for secondary actions, DANGER (red) for destructive actions, and SUCCESS (green) for positive actions. Defaults to PRIMARY.',
            },
            {
                content: 'ButtonType',
                hintText:
                    'Enum that determines the color scheme and visual styling',
            },
            { content: 'PRIMARY, SECONDARY, DANGER, SUCCESS' },
        ],
        [
            {
                content: 'size',
                hintText:
                    'Controls the dimensions and padding of the button. SMALL is compact, MEDIUM is standard, and LARGE is prominent. Size affects padding, font size, and overall button dimensions. Defaults to SMALL.',
            },
            {
                content: 'ButtonSize',
                hintText:
                    'Enum that controls the physical dimensions of the button',
            },
            { content: 'SMALL, MEDIUM, LARGE' },
        ],
        [
            {
                content: 'subType',
                hintText:
                    "Controls the button's layout and content arrangement. DEFAULT is standard with text and optional icons, ICON_ONLY displays only an icon (no text), and INLINE is a text-only variant for inline contexts. Defaults to DEFAULT.",
            },
            {
                content: 'ButtonSubType',
                hintText:
                    "Enum that determines the button's content layout style",
            },
            { content: 'DEFAULT, ICON_ONLY, INLINE' },
        ],
        [
            {
                content: 'text',
                hintText:
                    'Optional text string displayed inside the button. Required for DEFAULT and INLINE subtypes. For ICON_ONLY subtype, this is typically omitted. The text should be concise and action-oriented.',
            },
            {
                content: 'string',
                hintText: 'Text content displayed as the button label',
            },
            { content: '' },
        ],
        [
            {
                content: 'leadingIcon',
                hintText:
                    'Optional React element (typically an icon) displayed before the button text. Useful for providing visual context or indicating the action type. Works well with DEFAULT subtype buttons.',
            },
            {
                content: 'React.ReactNode',
                hintText:
                    'Any React element (icon, image, etc.) to display before the text',
            },
            { content: '' },
        ],
        [
            {
                content: 'trailingIcon',
                hintText:
                    'Optional React element (typically an icon) displayed after the button text. Useful for indicating direction (arrows), status, or additional information. Works well with DEFAULT subtype buttons.',
            },
            {
                content: 'React.ReactNode',
                hintText:
                    'Any React element (icon, image, etc.) to display after the text',
            },
            { content: '' },
        ],
        [
            {
                content: 'disabled',
                hintText:
                    "When true, the button becomes non-interactive and appears visually muted. Disabled buttons cannot be clicked and typically show reduced opacity. Useful for preventing actions when prerequisites aren't met. Defaults to false.",
            },
            {
                content: 'boolean',
                hintText:
                    'true disables interaction, false allows normal interaction',
            },
            { content: '' },
        ],
        [
            {
                content: 'onClick',
                hintText:
                    "Optional callback function invoked when the button is clicked. Receives no parameters and should handle the button's primary action. The button will be disabled during loading state.",
            },
            {
                content: '() => void',
                hintText: 'Function called when the user clicks the button',
            },
            { content: '' },
        ],
        [
            {
                content: 'loading',
                hintText:
                    'When true, the button displays a loading spinner and becomes non-interactive. Useful for async operations like form submissions or API calls. The button automatically disables itself during loading. Defaults to false.',
            },
            {
                content: 'boolean',
                hintText: 'true shows loading state, false shows normal state',
            },
            { content: '' },
        ],
        [
            {
                content: 'fullWidth',
                hintText:
                    "When true, the button expands to fill 100% of its container's width. Useful for full-width buttons in forms, modals, or narrow containers. Defaults to false (button only takes width needed for content).",
            },
            {
                content: 'boolean',
                hintText:
                    'true makes button full-width, false uses content-based width',
            },
            { content: '' },
        ],
        [
            {
                content: 'buttonGroupPosition',
                hintText:
                    'Controls border radius when used within a ButtonGroup. "left" removes right border radius, "right" removes left border radius, "center" removes both (for middle buttons). Undefined uses normal border radius. Used automatically by ButtonGroup.',
            },
            {
                content: "'center' | 'left' | 'right'",
                hintText: 'Union type that positions the button within a group',
            },
            { content: '' },
        ],
        [
            {
                content: 'justifyContent',
                hintText:
                    'CSS justify-content value for aligning button content horizontally. Controls how text and icons are positioned within the button. Common values: "center", "flex-start", "flex-end", "space-between". Defaults to "center".',
            },
            {
                content: 'CSSObject["justifyContent"]',
                hintText: 'CSS property value for horizontal content alignment',
            },
            { content: '' },
        ],
        [
            {
                content: 'state',
                hintText:
                    'Manually controls the visual state of the button for styling purposes. DEFAULT is normal, HOVER is hovered, ACTIVE is pressed/active, DISABLED is disabled. Typically controlled automatically by user interaction, but can be set manually. Defaults to DEFAULT.',
            },
            {
                content: 'ButtonState',
                hintText: 'Enum that determines the visual state styling',
            },
            { content: 'DEFAULT, HOVER, ACTIVE, DISABLED' },
        ],
    ]}
    className="mb-8"
    emptyMessage="No props available"
    loadingMessage="Loading props..."
/>

## Component Tokens

You can style the Button component using the following tokens:

```tsx
export type ButtonTokensType = {
    gap: CSSObject['gap']
    slotMaxHeight: {
        [key in ButtonSize]: CSSObject['maxHeight']
    }
    backgroundColor: {
        [key in ButtonType]: {
            [key in ButtonSubType]: {
                [key in
                    | 'default'
                    | 'hover'
                    | 'active'
                    | 'disabled']: CSSObject['background']
            }
        }
    }
    borderRadius: {
        [key in ButtonSize]: {
            [key in ButtonType]: {
                [key in ButtonSubType]: {
                    [key in
                        | 'default'
                        | 'hover'
                        | 'active'
                        | 'disabled']: CSSObject['borderRadius']
                }
            }
        }
    }
    padding: {
        [key in ButtonSize]: {
            [key in ButtonType]: {
                [key in ButtonSubType]: CSSObject['padding']
            }
        }
    }
    border: {
        [key in ButtonType]: {
            [key in ButtonSubType]: {
                [key in
                    | 'default'
                    | 'hover'
                    | 'active'
                    | 'disabled']: CSSObject['border']
            }
        }
    }
    shadow: {
        [key in ButtonType]: {
            [key in ButtonSubType]: {
                [key in
                    | 'default'
                    | 'hover'
                    | 'active'
                    | 'disabled']: CSSObject['boxShadow']
            }
        }
    }
    outline: {
        [key in ButtonType]: {
            [key in ButtonSubType]: {
                [key in
                    | 'default'
                    | 'hover'
                    | 'active'
                    | 'disabled']: CSSObject['outline']
            }
        }
    }
    text: {
        color: {
            [key in ButtonType]: {
                [key in ButtonSubType]: {
                    [key in
                        | 'default'
                        | 'hover'
                        | 'active'
                        | 'disabled']: CSSObject['color']
                }
            }
        }
        fontSize: {
            [key in ButtonSize]: CSSObject['fontSize']
        }
        fontWeight: {
            [key in ButtonSize]: CSSObject['fontWeight']
        }
    }
}
```
