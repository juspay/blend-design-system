---
title: Code Block
description: The CodeBlock component displays code snippets with syntax highlighting, line numbers, and diff view capabilities for showcasing code examples and changes.
category: components
tags:
    - code-block
    - component
    - code
    - syntax-highlighting
    - diff
    - code-snippet
RepoFolderName: CodeBlock
StorybookLink: components-codeblock--docs
version: 1
---

<CodeBlockPreview />

## Usage

```tsx
import {
    CodeBlock,
    CodeBlockVariant,
    DiffLineType,
} from '@juspay/blend-design-system'

function MyComponent() {
    const code = `function greet(name: string) {
    return \`Hello, \${name}!\`
}`

    return (
        <CodeBlock
            code={code}
            variant={CodeBlockVariant.DEFAULT}
            showLineNumbers={true}
            showHeader={true}
            header="example.ts"
        />
    )
}
```

## API Reference

<DocsTypeTable
    data={[
        [
            {
                content: 'code',
                hintText:
                    'Required string containing the code to display in the code block. The code is rendered with syntax highlighting, line numbers (if enabled), and proper formatting. For diff variant, use diffLines instead. The code string should contain the complete source code to be displayed.',
            },
            {
                content: 'string',
                hintText: 'Source code text to display in the code block',
            },
            { content: '' },
        ],
        [
            {
                content: 'variant',
                hintText:
                    'Controls the visual style and behavior of the code block. DEFAULT shows standard code with gutter and line numbers, NO_GUTTER removes the line number gutter for cleaner appearance, DIFF displays code changes with added/removed line highlighting. Defaults to DEFAULT.',
            },
            {
                content: 'CodeBlockVariant',
                hintText:
                    'Enum that determines the visual style and layout of the code block',
            },
            { content: 'DEFAULT, NO_GUTTER, DIFF' },
        ],
        [
            {
                content: 'showLineNumbers',
                hintText:
                    'Optional flag to explicitly control line number display. When true, shows line numbers in the gutter. When false, hides them. When undefined, line numbers are shown for DEFAULT variant and hidden for NO_GUTTER variant. DIFF variant always shows line numbers.',
            },
            {
                content: 'boolean',
                hintText: 'true shows line numbers, false hides them',
            },
            { content: '' },
        ],
        [
            {
                content: 'showHeader',
                hintText:
                    'When true, displays the header section with title, slots, and copy button. When false, hides the header completely, showing only the code content. Useful for embedded code blocks where header is handled separately. Defaults to true.',
            },
            {
                content: 'boolean',
                hintText:
                    'true shows header section, false hides header completely',
            },
            { content: '' },
        ],
        [
            {
                content: 'header',
                hintText:
                    'Optional text string displayed as the header title in the code block header. Typically shows the filename, language, or description of the code. If not provided and showHeader is true, defaults to "Header".',
            },
            {
                content: 'string',
                hintText: 'Text content for the code block header title',
            },
            { content: '' },
        ],
        [
            {
                content: 'headerLeftSlot',
                hintText:
                    'Optional React element displayed on the left side of the header, before the header text. Useful for adding file type icons, language indicators, or other visual elements that should appear at the start of the header.',
            },
            {
                content: 'ReactNode',
                hintText:
                    'Any React element to display on the left side of the header',
            },
            { content: '' },
        ],
        [
            {
                content: 'headerRightSlot',
                hintText:
                    'Optional React element displayed on the right side of the header, after the header text but before the copy button. Useful for adding action buttons, badges, or other controls that should appear in the header before the copy functionality.',
            },
            {
                content: 'ReactNode',
                hintText:
                    'Any React element to display on the right side of the header',
            },
            { content: '' },
        ],
        [
            {
                content: 'diffLines',
                hintText:
                    'Required array of DiffLine objects when variant is DIFF. Each object should have content (line text) and type (ADDED, REMOVED, or UNCHANGED). The diff view highlights added lines in green and removed lines in red, with appropriate gutter styling. Overrides the code prop when provided.',
            },
            {
                content: 'DiffLine[]',
                hintText:
                    'Array of objects with content and type (ADDED, REMOVED, UNCHANGED) properties',
            },
            { content: '' },
        ],
        [
            {
                content: 'showCopyButton',
                hintText:
                    'When true, displays the copy button in the header that allows users to copy the code to clipboard. When false, hides the copy button while keeping other header elements. Useful when you want to show code without allowing copying, or when copy functionality is handled externally. Defaults to true.',
            },
            {
                content: 'boolean',
                hintText: 'true shows copy button, false hides it',
            },
            { content: '' },
        ],
        [
            {
                content: 'autoFormat',
                hintText:
                    'When true, automatically formats the code before displaying. Uses a built-in formatter to handle indentation and spacing. Note: language prop must also be provided for autoFormat to work (supports: javascript, typescript, jsx, tsx, json, css, html). When false or when language is not provided, code is displayed as-is. Defaults to false.',
            },
            {
                content: 'boolean',
                hintText:
                    'true enables auto-formatting, false displays code as-is',
            },
            { content: '' },
        ],
        [
            {
                content: 'language',
                hintText:
                    'Optional string specifying the programming language for syntax highlighting and auto-formatting. Supported languages include: javascript, typescript, jsx, tsx, json, css, html, python, rust, haskell. The language is passed to the tokenizer to apply appropriate syntax highlighting colors. Required when using autoFormat. If not provided, falls back to typescript.',
            },
            {
                content: 'string',
                hintText:
                    'Programming language identifier for syntax highlighting',
            },
            { content: '' },
        ],
        [
            {
                content: 'maxHeight',
                hintText:
                    'Optional CSS value that sets the maximum height of the code content area. When the code exceeds this height, the content scrolls vertically while the header remains fixed. Accepts any valid CSS dimension such as "300px", "20rem", or "50vh". When not provided, the code block expands to fit all content without scrolling.',
            },
            {
                content: 'string',
                hintText:
                    'CSS max-height value (e.g. "300px", "20rem", "50vh")',
            },
            { content: '' },
        ],
    ]}
    className="mb-8"
    emptyMessage="No props available"
    loadingMessage="Loading props..."
/>

## Component Tokens

You can style the CodeBlock component using the following tokens:

```tsx
export type CodeBlockTokenType = {
    backgroundColor: CSSObject['backgroundColor']
    border: CSSObject['border']
    borderRadius: CSSObject['borderRadius']
    boxShadow: CSSObject['boxShadow']

    header: {
        backgroundColor: CSSObject['backgroundColor']
        borderBottom: CSSObject['borderBottom']
        padding: {
            x: CSSObject['padding']
            y: CSSObject['padding']
        }
        gap: CSSObject['gap']
        icon: {
            width: CSSObject['width']
        }
        text: {
            fontSize: CSSObject['fontSize']
            fontWeight: CSSObject['fontWeight']
            lineHeight: CSSObject['lineHeight']
            color: CSSObject['color']
        }
    }

    body: {
        padding: {
            x: CSSObject['padding']
            y: CSSObject['padding']
        }
        backgroundColor: CSSObject['backgroundColor']

        gutter: {
            width: CSSObject['width']
            color: CSSObject['color']
            backgroundColor: {
                [key in DiffLineType]: CSSObject['backgroundColor']
            }
            borderLeft: {
                [key in DiffLineType]: CSSObject['borderLeft']
            }
            borderColor: {
                [key in DiffLineType]: CSSObject['color']
            }
        }

        code: {
            fontFamily: CSSObject['fontFamily']
            fontSize: CSSObject['fontSize']
            lineHeight: CSSObject['lineHeight']
            padding: {
                x: {
                    left: CSSObject['paddingLeft']
                    right: CSSObject['paddingRight']
                }
                y: CSSObject['padding']
            }
        }

        highlightedLine: {
            backgroundColor: {
                [key in DiffLineType]: CSSObject['backgroundColor']
            }
        }

        syntax: {
            keyword: CSSObject['color']
            function: CSSObject['color']
            string: CSSObject['color']
            number: CSSObject['color']
            operator: CSSObject['color']
            variable: CSSObject['color']
            comment: CSSObject['color']
            text: CSSObject['color']
        }
    }
}

export type ResponsiveCodeBlockTokens = {
    [key in keyof BreakpointType]: CodeBlockTokenType
}
```
