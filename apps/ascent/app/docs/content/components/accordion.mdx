---
title: Accordion
description: The Accordion component creates collapsible content sections that can expand and collapse, helping to organize information hierarchically and save screen space.
category: components
tags:
    - accordion
    - component
    - collapsible
    - expandable
    - hierarchical
    - navigation
    - organization
RepoFolderName: Accordion
StorybookLink: components-accordion-accordion--docs
---

<AccordionPreview />

## Usage

```tsx
import {
    Accordion,
    AccordionItem,
    AccordionType,
    AccordionChevronPosition,
} from '@juspay/blend-design-system'

function MyComponent() {
    return (
        <Accordion accordionType={AccordionType.BORDER} isMultiple={false}>
            <AccordionItem value="item-1" title="Getting Started">
                <p>
                    Learn how to install and configure the design system in your
                    project.
                </p>
            </AccordionItem>
            <AccordionItem value="item-2" title="Components">
                <p>
                    Explore our comprehensive collection of reusable UI
                    components.
                </p>
            </AccordionItem>
            <AccordionItem value="item-3" title="Customization">
                <p>
                    Customize components using design tokens and theming
                    options.
                </p>
            </AccordionItem>
        </Accordion>
    )
}
```

## API Reference

### Accordion Props

<DocsTypeTable
    data={[
        [
            {
                content: 'children',
                hintText:
                    'React elements representing AccordionItem components to be rendered within the accordion. Each child should be an AccordionItem with a unique value prop.',
            },
            { content: 'ReactNode', hintText: 'React node' },
            { content: '' },
        ],
        [
            {
                content: 'accordionType',
                hintText:
                    'Controls the visual styling of the accordion. BORDER displays borders around each item, while NO_BORDER removes borders for a cleaner look. Defaults to NO_BORDER.',
            },
            { content: 'AccordionType', hintText: 'enum' },
            { content: 'BORDER, NO_BORDER' },
        ],
        [
            {
                content: 'defaultValue',
                hintText:
                    'Sets the initial expanded state for uncontrolled accordions. Can be a single string value or an array of strings for multiple items. Use this for uncontrolled components where you want to set initial state.',
            },
            {
                content: 'string | string[]',
                hintText:
                    'Single value for single accordion mode, or array for multiple mode',
            },
            { content: '' },
        ],
        [
            {
                content: 'value',
                hintText:
                    'Controlled value(s) that determine which accordion item(s) are currently expanded. Use with onValueChange for controlled accordion behavior. Must be a string for single mode or string[] for multiple mode.',
            },
            {
                content: 'string | string[]',
                hintText:
                    'Single value for single accordion mode, or array for multiple mode',
            },
            { content: '' },
        ],
        [
            {
                content: 'isMultiple',
                hintText:
                    'When true, allows multiple accordion items to be expanded simultaneously. When false, only one item can be expanded at a time, and expanding a new item will collapse the previously expanded one. Defaults to false.',
            },
            {
                content: 'boolean',
                hintText:
                    'true enables multiple expansion, false allows only one item expanded',
            },
            { content: '' },
        ],
        [
            {
                content: 'onValueChange',
                hintText:
                    'Callback function invoked whenever the expanded state changes. Receives the new value (string or string[]) as a parameter. Use this for controlled accordion behavior to synchronize state.',
            },
            {
                content: '(value: string | string[]) => void',
                hintText:
                    'Function that receives the new expanded value(s) when state changes',
            },
            { content: '' },
        ],
    ]}
    className="mb-8"
    emptyMessage="No props available"
    loadingMessage="Loading props..."
/>

### AccordionItem Props

<DocsTypeTable
    data={[
        [
            {
                content: 'value',
                hintText:
                    "Required unique identifier string for this accordion item. This value is used to track which item is expanded and must match the value used in the parent Accordion component's value or defaultValue prop.",
            },
            {
                content: 'string',
                hintText:
                    'Unique string identifier that must be unique across all accordion items',
            },
            { content: '' },
        ],
        [
            {
                content: 'title',
                hintText:
                    'Required main heading text displayed in the accordion trigger button. This is the primary text that users see and click to expand/collapse the item.',
            },
            {
                content: 'string',
                hintText: 'Text content for the accordion item heading',
            },
            { content: '' },
        ],
        [
            {
                content: 'children',
                hintText:
                    'Required content that appears when the accordion item is expanded. Can be any React node including text, components, or complex layouts. This content is hidden when the item is collapsed.',
            },
            {
                content: 'ReactNode',
                hintText:
                    'Any valid React content to display when item is expanded',
            },
            { content: '' },
        ],
        [
            {
                content: 'subtext',
                hintText:
                    'Optional secondary text displayed below the main title in the trigger. Useful for providing additional context or description without expanding the item.',
            },
            {
                content: 'string',
                hintText:
                    'Secondary descriptive text shown below the title in the trigger',
            },
            { content: '' },
        ],
        [
            {
                content: 'leftSlot',
                hintText:
                    'Optional React element displayed on the left side of the trigger, before the title. Commonly used for icons, badges, or other visual indicators.',
            },
            {
                content: 'ReactNode',
                hintText:
                    'Any React element to display on the left side of the trigger',
            },
            { content: '' },
        ],
        [
            {
                content: 'rightSlot',
                hintText:
                    'Optional React element displayed on the right side of the trigger, typically between the title and chevron icon. Useful for action buttons, badges, or status indicators.',
            },
            {
                content: 'ReactNode',
                hintText:
                    'Any React element to display on the right side of the trigger',
            },
            { content: '' },
        ],
        [
            {
                content: 'subtextSlot',
                hintText:
                    'Optional React element displayed alongside the subtext, typically on the right side. Useful for adding icons or small interactive elements next to the secondary text.',
            },
            {
                content: 'ReactNode',
                hintText: 'Any React element to display alongside the subtext',
            },
            { content: '' },
        ],
        [
            {
                content: 'isDisabled',
                hintText:
                    'When true, disables the accordion item, making it non-interactive. Disabled items cannot be expanded or collapsed and typically have reduced visual opacity. Defaults to false.',
            },
            {
                content: 'boolean',
                hintText:
                    'true disables interaction, false allows normal interaction',
            },
            { content: '' },
        ],
        [
            {
                content: 'chevronPosition',
                hintText:
                    'Controls the position of the expand/collapse chevron icon relative to the trigger content. LEFT places the chevron on the left side, RIGHT places it on the right side (default). The chevron automatically rotates to indicate expanded/collapsed state.',
            },
            { content: 'AccordionChevronPosition', hintText: 'enum' },
            { content: 'LEFT, RIGHT' },
        ],
        [
            {
                content: 'triggerSlot',
                hintText:
                    'Optional React element or render function to replace the default chevron icon. When provided, this custom element will be displayed instead of the default chevron. The slot container has a default width of 24px (from tokens), which can be overridden using triggerSlotWidth. If a render function is provided, it receives SlotRenderProps ({ isExpanded, toggle, value, isDisabled }) as props. The toggle function can be called to toggle the accordion state. If not provided, the default chevron icon will be used.',
            },
            {
                content: 'ReactNode | ((props: SlotRenderProps) => ReactNode)',
                hintText:
                    'Any React element to display as the trigger indicator (icon, text, or custom component), or a render function that receives SlotRenderProps',
            },
            { content: '' },
        ],
        [
            {
                content: 'triggerSlotWidth',
                hintText:
                    'Optional width value for the triggerSlot container. Overrides the default token value (24px). Accepts CSS width values as a string (e.g., "32px", "2rem") or a number (which will be converted to pixels). If not provided, uses the default token value.',
            },
            {
                content: 'string | number',
                hintText:
                    'CSS width value as string (e.g., "32px", "2rem") or number (converted to pixels)',
            },
            { content: '' },
        ],
    ]}
    className="mb-8"
    emptyMessage="No props available"
    loadingMessage="Loading props..."
/>

## SlotRenderProps

When using `triggerSlot` as a render function, you receive a `SlotRenderProps` object with the following properties:

```tsx
export type SlotRenderProps = {
    /** Whether this accordion item is currently expanded */
    isExpanded: boolean
    /** Function to toggle this accordion item's expansion state */
    toggle: () => void
    /** The value identifier of this accordion item */
    value: string
    /** Whether this accordion item is disabled */
    isDisabled: boolean
}
```

### Example Usage

```tsx
<AccordionItem
    value="item1"
    title="Custom Trigger"
    triggerSlot={({ isExpanded, toggle, value, isDisabled }) => (
        <Switch
            checked={isExpanded}
            onChange={() => toggle()}
            disabled={isDisabled}
            size={SwitchSize.SMALL}
            label=""
        />
    )}
>
    Content here
</AccordionItem>
```

## Component Tokens

You can style the accordion component using the following tokens:

```tsx
export type AccordionState =
    | 'default'
    | 'hover'
    | 'active'
    | 'disabled'
    | 'open'

export type AccordionTokenType = {
    gap: {
        [key in AccordionType]: CSSObject['gap']
    }
    borderRadius: {
        [key in AccordionType]: CSSObject['borderRadius']
    }
    trigger: {
        border: {
            [key in AccordionType]: {
                [key in AccordionState]?: CSSObject['border']
            }
        }
        padding: {
            [key in AccordionType]: CSSObject['padding']
        }
        backgroundColor: {
            [key in AccordionType]: {
                [key in AccordionState]?: CSSObject['backgroundColor']
            }
        }
        text: {
            title: {
                fontSize: CSSObject['fontSize']
                fontWeight: CSSObject['fontWeight']
                color: {
                    [key in AccordionState]?: CSSObject['color']
                }
            }
            subtext: {
                fontSize: CSSObject['fontSize']
                gap: CSSObject['gap']
                color: {
                    [key in AccordionState]?: CSSObject['color']
                }
            }
        }
    }
    separator: {
        color: {
            [key in AccordionType]: CSSObject['color']
        }
    }
    chevron: {
        width: CSSObject['width']
        height: CSSObject['height']
    }
}
```
