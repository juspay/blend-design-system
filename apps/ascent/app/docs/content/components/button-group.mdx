---
title: Button Group
description: The ButtonGroup component is a container that groups multiple Button components together with flexible layout options and automatic positioning for cohesive action interfaces.
category: components
tags:
    - button-group
    - component
    - layout
    - container
    - actions
RepoFolderName: ButtonGroup
StorybookLink: components-button-buttonv2--docs
---

<ButtonGroupPreview />

## Usage

```tsx
import { ButtonGroup, Button, ButtonType, ButtonSize } from 'blend-v1'

function MyComponent() {
    return (
        <ButtonGroup stacked={false}>
            <Button
                text="Cancel"
                buttonType={ButtonType.SECONDARY}
                size={ButtonSize.MEDIUM}
                onClick={() => console.log('Cancel clicked')}
            />
            <Button
                text="Save Changes"
                buttonType={ButtonType.PRIMARY}
                size={ButtonSize.MEDIUM}
                onClick={() => console.log('Save clicked')}
            />
        </ButtonGroup>
    )
}
```

## API Reference

<DocsTypeTable
    data={[
        [
            {
                content: 'stacked',
                hintText:
                    'Whether buttons should be stacked vertically or arranged horizontally',
            },
            { content: 'boolean', hintText: 'boolean' },
            { content: '' },
            { content: 'false' },
        ],
        [
            {
                content: 'children',
                hintText: 'Button components to be grouped together',
            },
            {
                content:
                    'ReactElement<ButtonProps> | ReactElement<ButtonProps>[]',
                hintText: 'React elements',
            },
            { content: '' },
            { content: 'Required' },
        ],
    ]}
    className="mb-8"
    emptyMessage="No props available"
    loadingMessage="Loading props..."
/>

## Component Tokens

You can style the ButtonGroup component using the following tokens:

```tsx
export type ButtonGroupTokenType = {
    gap: {
        horizontal: CSSObject['gap']
        stacked: CSSObject['gap']
    }
}
```
