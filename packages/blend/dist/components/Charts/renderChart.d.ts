import { RenderChartProps } from './types'
export declare const renderChart: ({
    flattenedData,
    chartType,
    hoveredKey,
    lineKeys,
    colors,
    setHoveredKey,
    xAxisLabel,
    yAxisLabel,
    data: originalData,
    selectedKeys,
}: RenderChartProps) => import('react/jsx-runtime').JSX.Element
