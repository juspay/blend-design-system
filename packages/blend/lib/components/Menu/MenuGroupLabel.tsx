import * as RadixMenu from "@radix-ui/react-dropdown-menu";
import styled from "styled-components";

const MenuGroupLabel = styled(RadixMenu.Label)(() => ({
  padding: "6px 8px",
  userSelect: "none",
  margin: "0px 6px",
}));

export default MenuGroupLabel;
