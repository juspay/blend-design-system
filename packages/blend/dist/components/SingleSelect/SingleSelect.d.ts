import { SingleSelectProps } from './types';
declare const SingleSelect: ({ label, subLabel, hintText, required, helpIconText, placeholder, size, items, name, variant, disabled, selected, onSelect, enableSearch, slot, alignment, side, sideOffset, alignOffset, minWidth, maxWidth, maxHeight, }: SingleSelectProps) => import("react/jsx-runtime").JSX.Element;
export default SingleSelect;
