---
version: 2
title: Multi Value Input
description: The MultiValueInputV2 component allows entering multiple values as tags with input validation.
category: Form Input
tags:
    - multi-value-input
    - component
    - input
    - tags
    - tokens
RepoFolderName: MultiValueInputV2
StorybookLink: components-inputs-multivalueinputv2--docs
---

<MultiValueInputV2Preview />

## Usage

```tsx
import { MultiValueInputV2, InputSizeV2 } from '@juspay/blend-design-system'
import { useState } from 'react'

function MyComponent() {
    const [value, setValue] = useState('')

    return (
        <MultiValueInputV2
            label="Tags"
            placeholder="Add tags..."
            value={value}
            onChange={setValue}
            size={InputSizeV2.MD}
            onTagAdd={(tag) => console.log('Added:', tag)}
            onTagRemove={(tag) => console.log('Removed:', tag)}
        />
    )
}
```

## API Reference

<DocsTypeTable
    data={[
        [
            {
                content: 'value',
                hintText: 'Current input value.',
            },
            {
                content: 'string',
                hintText: 'Input value',
            },
            { content: '' },
        ],
        [
            {
                content: 'onChange',
                hintText: 'Callback fired when input value changes.',
            },
            {
                content: '(value: string) => void',
                hintText: 'Change handler',
            },
            { content: '' },
        ],
        [
            {
                content: 'onTagAdd',
                hintText: 'Callback fired when a tag is added.',
            },
            {
                content: '(tag: string) => void',
                hintText: 'Tag add handler',
            },
            { content: '' },
        ],
        [
            {
                content: 'onTagRemove',
                hintText: 'Callback fired when a tag is removed.',
            },
            {
                content: '(tag: string) => void',
                hintText: 'Tag remove handler',
            },
            { content: '' },
        ],
        [
            {
                content: 'label',
                hintText: 'Field label displayed above the input.',
            },
            {
                content: 'string',
                hintText: 'Label text',
            },
            { content: '' },
        ],
        [
            {
                content: 'placeholder',
                hintText: 'Placeholder text when empty.',
            },
            {
                content: 'string',
                hintText: 'Placeholder content',
            },
            { content: '' },
        ],
        [
            {
                content: 'size',
                hintText: 'Size affecting tag and input sizing.',
            },
            {
                content: 'InputSizeV2',
                hintText: 'Enum for input size',
            },
            { content: 'SM, MD, LG' },
        ],
        [
            {
                content: 'disabled',
                hintText: 'When true, input is non-interactive.',
            },
            {
                content: 'boolean',
                hintText: 'true disables the input',
            },
            { content: '' },
        ],
        [
            {
                content: 'error',
                hintText: 'Error state configuration.',
            },
            {
                content: '{ show: boolean; message?: string }',
                hintText: 'Error display settings',
            },
            { content: '' },
        ],
    ]}
    className="mb-8"
    emptyMessage="No props available"
    loadingMessage="Loading props..."
/>

## Component Tokens

You can style the MultiValueInputV2 component using the following tokens:

```tsx
export type MultiValueInputV2TokensType = {
    container: {
        padding: CSSObject['padding']
        borderRadius: CSSObject['borderRadius']
        border: {
            default: CSSObject['border']
            focus: CSSObject['border']
            error: CSSObject['border']
            disabled: CSSObject['border']
        }
        background: {
            default: CSSObject['background']
            disabled: CSSObject['background']
        }
        gap: CSSObject['gap']
    }
    tag: {
        padding: CSSObject['padding']
        borderRadius: CSSObject['borderRadius']
        background: CSSObject['background']
        color: CSSObject['color']
        fontSize: CSSObject['fontSize']
        gap: CSSObject['gap']
        removeButton: {
            color: CSSObject['color']
            hoverColor: CSSObject['color']
            size: CSSObject['width']
        }
    }
    input: {
        minWidth: CSSObject['minWidth']
        fontSize: CSSObject['fontSize']
        color: CSSObject['color']
    }
}
```
