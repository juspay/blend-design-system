/**
 * Only used by Jest — Metro reads the consuming app's babel config, and the
 * published package ships source that the consumer's own transform handles.
 */
module.exports = {
    presets: ['babel-preset-expo'],
}
