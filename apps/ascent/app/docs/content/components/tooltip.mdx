---
title: Tooltip
description: The Tooltip component displays additional information when users hover over or focus on elements, providing contextual help and explanations without cluttering the interface.
category: components
tags:
    - tooltip
    - component
    - help
    - hover
    - contextual
RepoFolderName: Tooltip
StorybookLink: components-tooltip-tooltip--docs
---

<TooltipPreview />

## Usage

```tsx
import {
    Tooltip,
    TooltipSide,
    TooltipAlign,
    TooltipSize,
    TooltipSlotDirection,
} from 'blend-v1'

function MyComponent() {
    return (
        <Tooltip
            content="This is a helpful tooltip"
            side={TooltipSide.TOP}
            align={TooltipAlign.CENTER}
            size={TooltipSize.SMALL}
            showArrow={true}
            delayDuration={300}
        >
            <button>Hover me</button>
        </Tooltip>
    )
}
```

## API Reference

<DocsTypeTable
    data={[
        [
            {
                content: 'children',
                hintText:
                    'The trigger element that will show the tooltip on hover/focus',
            },
            { content: 'ReactNode', hintText: 'React node' },
            { content: 'Required' },
        ],
        [
            {
                content: 'content',
                hintText: 'The content to display in the tooltip',
            },
            { content: 'ReactNode | string', hintText: 'React node or string' },
            { content: 'Required' },
        ],
        [
            {
                content: 'side',
                hintText:
                    'The side of the trigger element where the tooltip appears',
            },
            { content: 'TooltipSide', hintText: 'enum' },
            { content: 'TooltipSide.TOP' },
        ],
        [
            {
                content: 'align',
                hintText:
                    'The alignment of the tooltip relative to the trigger element',
            },
            { content: 'TooltipAlign', hintText: 'enum' },
            { content: 'TooltipAlign.CENTER' },
        ],
        [
            {
                content: 'showArrow',
                hintText:
                    'Whether to show the arrow pointing to the trigger element',
            },
            { content: 'boolean', hintText: 'boolean' },
            { content: 'true' },
        ],
        [
            { content: 'size', hintText: 'The size variant of the tooltip' },
            { content: 'TooltipSize', hintText: 'enum' },
            { content: 'TooltipSize.SMALL' },
        ],
        [
            {
                content: 'slot',
                hintText:
                    'Optional icon or element to display alongside the tooltip content',
            },
            { content: 'ReactNode', hintText: 'React node' },
            { content: 'undefined' },
        ],
        [
            {
                content: 'slotDirection',
                hintText:
                    'The position of the slot relative to the tooltip content',
            },
            { content: 'TooltipSlotDirection', hintText: 'enum' },
            { content: 'TooltipSlotDirection.LEFT' },
        ],
        [
            {
                content: 'delayDuration',
                hintText: 'Delay in milliseconds before showing the tooltip',
            },
            { content: 'number', hintText: 'number' },
            { content: '300' },
        ],
        [
            {
                content: 'offset',
                hintText:
                    'Distance between the tooltip and the trigger element',
            },
            { content: 'number', hintText: 'number' },
            { content: '5' },
        ],
        [
            {
                content: 'open',
                hintText: 'Controlled state for showing/hiding the tooltip',
            },
            { content: 'boolean', hintText: 'boolean' },
            { content: 'undefined' },
        ],
        [
            {
                content: 'maxWidth',
                hintText: 'Maximum width of the tooltip',
            },
            { content: 'string', hintText: 'string' },
            { content: 'undefined' },
        ],
    ]}
    className="mb-8"
    emptyMessage="No props available"
    loadingMessage="Loading props..."
/>

## Features

- Multiple positioning options (top, right, bottom, left)
- Flexible alignment (start, center, end)
- Two size variants (small, large)
- Optional arrow indicator
- Icon slot support with left/right positioning
- Configurable delay and offset
- Controlled and uncontrolled modes
- Accessible by default
- Customizable styling through tokens

## Component Tokens

You can style the Tooltip component using the following tokens:

```tsx
export type TooltipTokensType = {
    background: CSSObject['backgroundColor']
    borderRadius: {
        [key in TooltipSize]: CSSObject['borderRadius']
    }
    maxWidth: {
        [key in TooltipSize]: CSSObject['maxWidth']
    }
    padding: {
        [key in TooltipSize]: CSSObject['padding']
    }
    gap: {
        [key in TooltipSize]: CSSObject['gap']
    }
    text: {
        color: CSSObject['color']
        fontWeight: {
            [key in TooltipSize]: CSSObject['fontWeight']
        }
        fontSize: {
            [key in TooltipSize]: CSSObject['fontSize']
        }
        lineHeight: {
            [key in TooltipSize]: CSSObject['lineHeight']
        }
    }
}

export type ResponsiveTooltipTokens = {
    [key in keyof BreakpointType]: TooltipTokensType
}
```

## Enums

### TooltipSide

```tsx
enum TooltipSide {
    TOP = 'top',
    RIGHT = 'right',
    LEFT = 'left',
    BOTTOM = 'bottom',
}
```

### TooltipAlign

```tsx
enum TooltipAlign {
    START = 'start',
    END = 'end',
    CENTER = 'center',
}
```

### TooltipSize

```tsx
enum TooltipSize {
    SMALL = 'sm',
    LARGE = 'lg',
}
```

### TooltipSlotDirection

```tsx
enum TooltipSlotDirection {
    LEFT = 'left',
    RIGHT = 'right',
}
```
